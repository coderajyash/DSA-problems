import java.util.*;
public class Helper {

    public static void main(String[] atgs) {
        //---------------------------ArrayList--------------------------------------------------
        ArrayList<Integer> al = new ArrayList<>();

        al.add(2);
        al.add(1,3);
        al.get(index);
        al.remove(1);
        al.contains(2);
        al.size();
        al.toString();
        al.toArray();
        
        //---------------------------LinkedList-------------------------------------------------

        LinkedList<Integer> ll = new LinkedList<>();
        ll.add(e);
        ll.addFirst(e);
        ll.addLast(e);
        ll.size();
        ll.contains(o);
        ll.remove(e);
        ll.peek();
        ll.peekFirst();
        ll.peekLast();
        ll.poll();//removes and returns first element
        ll.remove();//removes and returns first element
        ll.remove(index);
        ll.removeFirst();
        ll.removeLast();

        //
    }
    
}
